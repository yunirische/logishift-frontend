---
phase: 09-shift-modal-data-integrity
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/EditShiftModal.tsx
autonomous: true

must_haves:
  truths:
    - "History tab in EditShiftModal loads and displays audit trail data from GET /api/v1/audit/shift/:id"
    - "History tab uses timeline list layout with timestamp left-aligned, action description right-aligned"
    - "History tab metadata shows: '14 Feb 12:30 • User Name • Action' format"
    - "History entries have visual icons for different action types (edit, delete, status change)"
    - "History tab shows loading skeleton while fetching audit data (200ms delay before skeleton)"
  artifacts:
    - path: "src/components/EditShiftModal.tsx"
      provides: "EditShiftModal with tabbed interface including History tab for audit trail"
      min_lines: 850
  key_links:
    - from: "src/components/EditShiftModal.tsx"
      to: "API_ENDPOINTS.AUDIT_SHIFT(shift.id)"
      via: "loadAuditLogs function fetches audit trail data"
      pattern: "api\\.get\\(API_ENDPOINTS\\.AUDIT_SHIFT"
    - from: "src/components/EditShiftModal.tsx"
      to: "History tab state"
      via: "useState for activeTab, auditLogs loading state"
      pattern: "useState.*activeTab|useState.*loadingAudit"
    - from: "src/components/EditShiftModal.tsx"
      to: "Timeline rendering"
      via: "Map auditLogs array to timeline entries with icons"
      pattern: "auditLogs\\.map.*timestamp.*action"
---

<objective>
Add History tab to EditShiftModal with timeline layout displaying audit trail data from API.

Purpose: Per MODAL-01 requirement, History tab must load and display audit trail from GET /api/v1/audit/shift/:id. Current EditShiftModal loads audit logs inline but lacks proper tabbed interface. This plan adds tab structure, timeline layout per user decisions (timestamp left, description right), visual icons for action types, and loading skeleton.

Output: Updated EditShiftModal with History tab showing chronological audit trail with timeline layout, icons, and loading state.
</objective>

<execution_context>
@C:/Users/1/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/1/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/09-shift-modal-data-integrity/09-CONTEXT.md

@src/components/EditShiftModal.tsx
@src/constants.ts
@src/types.ts
@.planning/phases/08-driver-ui-unification/08-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Add tabbed interface structure to EditShiftModal</name>
  <files>src/components/EditShiftModal.tsx</files>
  <action>
    Current EditShiftModal displays comments and audit history inline in one section. Convert to tabbed interface:

    1. Add activeTab state:
       const [activeTab, setActiveTab] = useState<'details' | 'history' | 'comments'>('details');

    2. Keep existing time fields and photo upload zones in 'details' tab (default view)

    3. Add tab buttons below photo section or above comments/history:
       - Tab 1: "Редактирование" (Editing) - shows time fields + photo zones
       - Tab 2: "История" (History) - shows audit trail timeline
       - Tab 3: "Комментарии" (Comments) - shows comments + new comment input

    4. Tab button styling:
       - Flex container with gap-2
       - Each button: px-4 py-2 text-sm font-semibold rounded-lg
       - Active tab: bg-[#0a192f] text-white
       - Inactive: bg-slate-100 text-slate-600 hover:bg-slate-200

    5. Update existing comments section (lines 670-745) to only render when activeTab === 'comments'

    6. Move audit history section (lines 707-725) to only render when activeTab === 'history'

    The goal: Add tab structure without changing existing data fetching logic.
  </action>
  <verify>
    1. Check that activeTab state exists with 'details' | 'history' | 'comments' type
    2. Check that tab buttons render and switch between tabs
    3. Check that comments section only renders when activeTab === 'comments'
    4. Check that audit history section only renders when activeTab === 'history'
  </verify>
  <done>
    EditShiftModal has tabbed interface with three tabs: Editing, History, Comments. Existing sections render conditionally based on active tab.
  </done>
</task>

<task type="auto">
  <name>Implement History tab with timeline layout and audit trail display</name>
  <files>src/components/EditShiftModal.tsx</files>
  <action>
    History tab should display audit trail data with timeline layout per user decisions:

    1. Add loading state for audit data:
       const [loadingAudit, setLoadingAudit] = useState(false);
       const [showAuditSkeleton, setShowAuditSkeleton] = useState(false);

    2. Update loadAuditLogs (lines 113-127) with loading states:
       - Set setLoadingAudit(true) on start
       - After 200ms delay, setShowAuditSkeleton(true) if still loading
       - Set both false when complete or error

    3. History tab content structure:
       - Header: "История изменений" with History icon
       - Loading skeleton (when showAuditSkeleton):
         * 3-5 timeline rows with shimmer animation
         * Each skeleton: h-4 bg-slate-200 rounded animate-pulse
       - Timeline (when !loadingAudit && auditLogs.length > 0):
         * Single column, max-h-64 overflow-y-auto
         * Each entry: flex row, timestamp left, description right
         * Format: "14 Feb 12:30 • User Name • Action"
         * Timestamp: font-mono text-xs text-slate-400, left-aligned
         * Icon: action-specific (edit=Pencil, delete=Trash, etc.)
         * User name: font-semibold text-slate-600
         * Action: text-slate-500
       - Empty state: "История изменений пуста" with slate-400 text
       - Error state: red text showing "Ошибка загрузки истории"

    4. Timeline styling per user decision:
       - Border-left: 2px border-slate-200
       - Each entry: pl-4 relative
       - Timeline dot: absolute left-[-5px] top-1 w-2 h-2 rounded-full bg-slate-400
       - Timestamp: text-[10px] font-mono text-slate-400
       - Spacing: space-y-3

    5. Map action types to icons (using lucide-react icons):
       - Edit/EditShift -> Pencil
       - Delete/Remove -> Trash2
       - Status change -> ArrowRightLeft
       - Photo upload -> Image
       - Comment -> MessageSquare
       - Default -> FileText

    The key: Timeline format per user decision with left-aligned timestamps, right-aligned descriptions, visual icons for actions.
  </action>
  <verify>
    1. Check that loadingAudit and showAuditSkeleton states exist
    2. Check that 200ms delay logic exists before showing skeleton
    3. Check that History tab renders timeline with correct structure
    4. Check that timestamp format matches "14 Feb 12:30" pattern
    5. Check that icons are mapped to action types
  </verify>
  <done>
    History tab displays chronological audit trail with timeline layout. Loading skeleton shows after 200ms delay. Empty/error states handled.
  </done>
</task>

<task type="auto">
  <name>Integrate audit trail API data fetching with tab-specific loading</name>
  <files>src/components/EditShiftModal.tsx</files>
  <action>
    Ensure audit trail data loads correctly with tab-specific loading:

    1. Verify loadAuditLogs function (lines 113-127) fetches from correct endpoint:
       - Uses: API_ENDPOINTS.AUDIT_SHIFT(shift.id)
       - This resolves to: /api/v1/audit/shift/:id

    2. Add useEffect to trigger audit load when History tab becomes active:
       useEffect(() => {
         if (activeTab === 'history' && auditLogs.length === 0) {
           loadAuditLogs();
         }
       }, [activeTab]);

    3. Ensure data structure handling:
       - Check for Array.isArray(data) response
       - Check for data.logs format
       - Default to empty array on error

    4. Update the existing useEffect (lines 55-78) that loads on modal open:
       - Remove loadAuditLogs() from initial open effect
       - Only load comments/time fields on open
       - Let History tab load audit data lazily when clicked

    5. Add retry logic for audit fetch:
       - Add "Retry" button in error state
       - onClick: loadAuditLogs()

    The goal: Tab-specific loading means audit data only fetches when History tab is active, not on modal open.
  </action>
  <verify>
    1. Check that loadAuditLogs uses API_ENDPOINTS.AUDIT_SHIFT
    2. Check that useEffect exists for History tab activation
    3. Check that initial modal open no longer calls loadAuditLogs
    4. Check that error state includes Retry button
  </verify>
  <done>
    History tab loads audit data on activation, not modal open. Retry button available on error.
  </done>
</task>

</tasks>

<verification>
Phase verification:

1. Tab structure:
   - Open EditShiftModal
   - Verify three tabs exist: Editing, History, Comments
   - Verify clicking switches active tab
   - Verify correct section displays per tab

2. History tab functionality:
   - Click History tab
   - Verify loading skeleton appears after 200ms if data loading
   - Verify timeline displays with left-aligned timestamps
   - Verify format matches "14 Feb 12:30 • User Name • Action"
   - Verify icons appear for different action types
   - Verify empty state message when no history
   - Verify Retry button appears on error

3. Data correctness:
   - Verify API call to /api/v1/audit/shift/:id is made
   - Verify audit trail data displays chronologically
   - Verify scrolling works if many entries
</verification>

<success_criteria>
1. History tab loads and displays audit trail data from GET /api/v1/audit/shift/:id
2. Timeline layout: single column, timestamp left-aligned, description right-aligned
3. Metadata format: "14 Feb 12:30 • User Name • Action"
4. Visual icons distinguish action types (edit, delete, status change, etc.)
5. Loading skeleton shows after 200ms delay with shimmer animation
6. Tab-specific loading: audit data fetches on History tab activation, not modal open
7. Empty and error states handled appropriately
</success_criteria>

<output>
After completion, create `.planning/phases/09-shift-modal-data-integrity/09-01-SUMMARY.md`
</output>
