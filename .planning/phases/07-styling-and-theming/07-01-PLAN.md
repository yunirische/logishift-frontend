---
phase: 07-styling-and-theming
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/analytics/TrendsChart.tsx
  - src/components/analytics/DriverRankings.tsx
  - src/components/analytics/UsageCard.tsx
  - src/components/analytics/InsightsPanel.tsx
  - src/index.css
autonomous: true

must_haves:
  truths:
    - "All chart labels, axes, and tooltips use JetBrains Mono font family"
    - "Chart bars use Navy/Indigo color palette (indigo-600 to indigo-900)"
    - "All cards use rounded-3xl styling with consistent spacing (p-6)"
    - "All controls use Lucide React icons (TrendingUp, Trophy, Info, AlertTriangle, AlertCircle, RefreshCw, etc.)"
  artifacts:
    - path: "src/components/analytics/TrendsChart.tsx"
      provides: "Chart with JetBrains Mono typography on axes and tooltips"
      contains: "font-mono"
    - path: "src/index.css"
      provides: "Global Recharts typography overrides using JetBrains Mono"
      contains: "@layer components"
  key_links:
    - from: "src/index.css"
      to: "recharts components"
      via: "Global CSS selectors targeting .recharts-text, .recharts-tooltip"
      pattern: "\.recharts.*font-mono"
---

<objective>
Apply JetBrains Mono font to all Recharts chart elements (axes labels, tick values, tooltips) and verify consistent styling across all analytics components. This is the final polish phase - all functionality is complete from Phases 1-6.

Purpose: Ensure the analytics dashboard matches the LogiShift industrial design aesthetic with monospace typography on data visualizations, consistent with other numeric displays in the app (shift tables, truck plates, etc.).

Output: Fully styled analytics dashboard with JetBrains Mono on all chart text elements and verified consistency across spacing, colors, and icons.
</objective>

<execution_context>
@C:\Users\1\.claude\get-shit-done\workflows\execute-plan.md
@C:\Users\1\.claude\get-shit-done\templates\summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@src/tailwind.config.js
@src/index.css
@src/components/analytics/TrendsChart.tsx
@src/components/analytics/DriverRankings.tsx
@src/components/analytics/UsageCard.tsx
@src/components/analytics/InsightsPanel.tsx
</context>

<tasks>

<task type="auto">
  <name>Apply JetBrains Mono font to Recharts typography</name>
  <files>src/index.css, src/components/analytics/TrendsChart.tsx</files>
  <action>
    1. In src/index.css, add a new @layer components section with global Recharts typography overrides:
       - Target .recharts-cartesian-axis-tick-value for axis labels
       - Target .recharts-tooltip-content for tooltip text
       - Apply font-mono class (which maps to JetBrains Mono)
       - Use @apply directive for Tailwind integration

    2. In TrendsChart.tsx, enhance the CustomTooltip component to ensure font-mono is applied:
       - Add font-mono class to numeric values in tooltip
       - Ensure consistent font sizing with Tailwind classes

    Do NOT change any existing functionality, data flow, or component behavior. Only modify typography-related styling.

    Recharts components (XAxis, YAxis, Tooltip) use inline styles and SVG elements, so the CSS selector approach is required. The custom tooltip already uses React/HTML elements and can use Tailwind classes directly.
  </action>
  <verify>
    1. Run `npm run dev` and navigate to the Analytics tab
    2. Check browser DevTools Elements panel to verify .recharts-cartesian-axis-tick-value elements have font-family: "JetBrains Mono"
    3. Hover over chart bars and verify tooltip text uses JetBrains Mono
  </verify>
  <done>
    Chart axis labels and tooltips display with JetBrains Mono font family. All numeric data in the chart (date labels, metric values, K-suffix values) uses monospace typography.
  </done>
</task>

<task type="auto">
  <name>Verify styling consistency across all analytics components</name>
  <files>src/components/analytics/TrendsChart.tsx, src/components/analytics/DriverRankings.tsx, src/components/analytics/UsageCard.tsx, src/components/analytics/InsightsPanel.tsx</files>
  <action>
    1. Verify each component uses rounded-3xl for card containers (already in place from Phases 1-6)
    2. Verify each component uses p-6 for consistent internal padding (already in place)
    3. Verify indigo color palette is used consistently:
       - Icons: indigo-50 background, indigo-600 color
       - Metric tabs: indigo-600 active state
       - Chart bars: indigo-600, indigo-700, indigo-800 based on metric
    4. Verify all icons are from lucide-react (already in place):
       - TrendsChart: TrendingUp, AlertCircle, RefreshCw
       - DriverRankings: Trophy, ChevronUp, ChevronDown, AlertCircle, RefreshCw
       - UsageCard: uses icon prop from lucide-react (Truck, Users, Building2 passed from parent)
       - InsightsPanel: AlertTriangle, TrendingDown, Info, AlertCircle, RefreshCw

    If any inconsistencies are found, correct them. Based on prior phase context, all of these should already be consistent - this task is primarily verification.

    Note: UsageCard, InsightsPanel use emerald/amber/orange for progress bars and alert colors - this is by design and should NOT be changed. Only the indigo accent colors (icons, active states) need verification.
  </action>
  <verify>
    1. Run `npm run dev` and navigate to Analytics tab
    2. Visually inspect all cards have consistent rounded corners and padding
    3. Check browser DevTools to verify all icon components are from lucide-react
    4. Confirm chart bars use indigo-600/700/800 fill colors
  </verify>
  <done>
    All analytics components use consistent styling: rounded-3xl cards, p-6 padding, indigo accent colors, and Lucide React icons throughout. No styling inconsistencies remain.
  </done>
</task>

</tasks>

<verification>
Overall verification criteria for Phase 7:

1. Run `npm run dev` and navigate to the Analytics page
2. Open browser DevTools and inspect chart elements:
   - Axis labels should have font-family: "JetBrains Mono", monospace
   - Tooltip content should use JetBrains Mono
3. Visually verify all cards have matching rounded corners and spacing
4. Confirm all icons render correctly (Lucide React icons, not broken or missing)
5. Test on mobile viewport to ensure consistent styling at smaller sizes

This phase is complete when the analytics dashboard has a polished, professional appearance that matches the existing LogiShift design system.
</verification>

<success_criteria>
1. All Recharts typography (axes, tooltips) uses JetBrains Mono font family
2. Chart visualizations use the Navy/Indigo color palette consistently
3. Card containers all use rounded-3xl with p-6 for consistent spacing
4. All interactive controls and indicators use Lucide React icons
5. Visual inspection confirms the analytics dashboard matches the industrial aesthetic of the rest of LogiShift
</success_criteria>

<output>
After completion, create `.planning/phases/07-styling-and-theming/07-01-SUMMARY.md` with:
- Typography changes made (Recharts CSS overrides, CustomTooltip updates)
- Any styling inconsistencies found and corrected
- Confirmation that Phase 7 completes the v1.5 Analytics Dashboard milestone
</output>
