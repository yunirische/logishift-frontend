---
phase: 08-driver-ui-unification
plan: 03
type: execute
wave: 3
depends_on: [08-01]
files_modified:
  - src/views/DriverView.tsx
  - src/components/ShiftHistoryModal.tsx
autonomous: true

must_haves:
  truths:
    - "Drivers can view their shift history within the driver interface"
    - "Compact section shows 5 most recent shifts below main driver controls"
    - "View More button opens modal with full chronological shift history"
    - "Each shift row shows: Date, Truck Plate, Site, Hours"
  artifacts:
    - path: "src/views/DriverView.tsx"
      provides: "Driver interface with shift history section and View More button"
      min_lines: 450
    - path: "src/components/ShiftHistoryModal.tsx"
      provides: "Modal component displaying full shift history in chronological order"
      min_lines: 80
  key_links:
    - from: "src/views/DriverView.tsx"
      to: "/api/v1/shifts?driver_id={id}&status=completed&limit=10"
      via: "Fetch shift history on component mount"
      pattern: "api\\.get\\(['\"].*shifts.*driver_id"
    - from: "src/components/ShiftHistoryModal.tsx"
      to: "src/views/DriverView.tsx"
      via: "Import and render modal when View More is clicked"
      pattern: "ShiftHistoryModal.*isOpen"
---

<objective>
Add shift history visibility to driver interface with compact view and full history modal.

Purpose: Drivers need to see their past shifts for reference. A compact section below main controls shows 5 most recent shifts. A "View More" button opens a modal with the full chronological list. This provides quick access to recent history without cluttering the main workflow.

Output: Updated DriverView with shift history section; new ShiftHistoryModal component.
</objective>

<execution_context>
@C:/Users/1/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/1/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/08-driver-ui-unification/08-CONTEXT.md

@src/views/DriverView.tsx
@src/components/ui.tsx
@.planning/phases/08-driver-ui-unification/08-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Create ShiftHistoryModal component for full shift history display</name>
  <files>src/components/ShiftHistoryModal.tsx</files>
  <action>
    Create a new modal component src/components/ShiftHistoryModal.tsx:

    Props interface:
    - isOpen: boolean
    - onClose: () => void
    - shifts: array (shift history data)

    Modal structure:
    1. Overlay: fixed inset-0 z-50 bg-black/50 flex items-center justify-center p-4
    2. Content: bg-white rounded-xl max-w-md w-full max-h-[80vh] overflow-hidden flex flex-col
    3. Header: Title "Мои смены" (My Shifts) with close button
    4. List: Scrollable container with shift rows
    5. Row format per shift:
       - Date: {shift.created_at} formatted as "DD MMM" (e.g., "15 фев")
       - Truck: {shift.truck?.name || shift.truck_name || '—'}
       - Site: {shift.site?.name || shift.site_name || '—'}
       - Hours: {shift.hours_worked || '—'}
    6. Empty state: "У вас пока нет завершенных смен" (No completed shifts yet)

    Styling:
    - Use existing Card patterns from ui.tsx
    - Navy-900 theme colors consistent with app
    - Responsive design for mobile-first
    - Each row: p-3 bg-slate-50 rounded-lg border border-slate-100

    Export as default: export default ShiftHistoryModal
  </action>
  <verify>
    1. Check src/components/ShiftHistoryModal.tsx exists
    2. Verify component has proper TypeScript props interface
    3. Verify modal structure matches specification
  </verify>
  <done>
    ShiftHistoryModal component exists and displays full shift history in modal dialog.
  </done>
</task>

<task type="auto">
  <name>Integrate shift history section and View More button into DriverView</name>
  <files>src/views/DriverView.tsx</files>
  <action>
    DriverView already has shift history state and API call (lines 26, 50). Add:

    1. Add state for modal visibility:
       const [showHistoryModal, setShowHistoryModal] = useState(false);

    2. Import ShiftHistoryModal at top of file

    3. The shift history section already exists (lines 377-409) but may need:
       - Add "View More" button below the 5-item compact list
       - Button text: "Показать все смены" (Show all shifts)
       - Button onClick: () => setShowHistoryModal(true)
       - Style: secondary button (outline or lighter bg than main CTA)

    4. Add modal rendering at bottom of component (before closing div):
       {showHistoryModal && (
         <ShiftHistoryModal
           isOpen={showHistoryModal}
           onClose={() => setShowHistoryModal(false)}
           shifts={shiftHistory}
         />
       )}

    5. Ensure shift history only shows when no active shift:
       - Currently in conditional: {!activeShift || user?.current_state === "idle"}
       - Keep it there so history is only visible when not in active shift

    The shift history fetch already exists:
    - api.get("/shifts?driver_id=" + user?.id + "&status=completed&limit=10")
    - Store result in shiftHistory state

    Key requirement: "View More" button triggers modal showing full list.
  </action>
  <verify>
    1. Check that ShiftHistoryModal is imported
    2. Check that showHistoryModal state exists
    3. Check that View More button exists and sets state to true
    4. Check that modal renders when state is true
  </verify>
  <done>
    Driver interface shows 5 most recent shifts with "View More" button opening full history modal.
  </done>
</task>

</tasks>

<verification>
Shift history verification:

1. Compact section visibility:
   - When no active shift, scroll below main controls
   - Verify "Мои последние смены" (My Recent Shifts) section exists
   - Verify up to 5 shifts displayed with date, truck, site
   - Verify "View More" button exists below list

2. Modal functionality:
   - Click "View More" - verify modal opens
   - Verify modal shows full chronological list (all fetched shifts)
   - Verify close button closes modal
   - Verify clicking outside modal closes it

3. Empty state:
   - For new drivers with no completed shifts, verify empty state message
   - Verify "View More" button only shows when shifts exist
</verification>

<success_criteria>
1. Shift history section displays in DriverView when no active shift
2. Compact view shows 5 most recent completed shifts
3. "View More" button opens modal with full shift history
4. Modal displays chronological list with Date, Truck, Site, Hours per shift
5. Empty state handles drivers with no completed shifts
</success_criteria>

<output>
After completion, create `.planning/phases/08-driver-ui-unification/08-03-SUMMARY.md`
</output>
