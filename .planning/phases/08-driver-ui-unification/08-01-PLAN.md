---
phase: 08-driver-ui-unification
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/Dashboard.tsx
  - src/views/DriverView.tsx
  - src/App.tsx
autonomous: true

must_haves:
  truths:
    - "DriverView component is the single source of truth for all driver interfaces regardless of user role (ADMIN/FOREMAN/DRIVER)"
    - "Admins/foremen can access DriverView from dashboard to see their own shift interface"
    - "Driver UI shows Timer/Finish button immediately after Start Shift is clicked (no page reload required)"
    - "User state (current_state) is synchronized with backend after shift actions"
    - "Dashboard stats (active shifts count) refresh every 60 seconds via polling"
    - "Dashboard stats refresh immediately when window/tab regains focus"
  artifacts:
    - path: "src/components/Dashboard.tsx"
      provides: "Role-based routing with DriverView access for all roles"
      min_lines: 100
    - path: "src/views/DriverView.tsx"
      provides: "Unified driver interface with real-time state sync"
      min_lines: 400
    - path: "src/App.tsx"
      provides: "Navigation entry point for driver view access"
      min_lines: 50
  key_links:
    - from: "src/components/Dashboard.tsx"
      to: "src/views/DriverView.tsx"
      via: "Direct import and conditional rendering based on user role"
      pattern: "import.*DriverView.*from.*views/DriverView"
    - from: "src/views/DriverView.tsx"
      to: "/api/v1/shifts/start"
      via: "POST request to start shift, then refetch current shift state"
      pattern: "api\\.post\\(['\"].*shifts/start"
    - from: "src/views/DriverView.tsx"
      to: "AuthContext.refreshUser"
      via: "Call after shift actions to sync user state with backend"
      pattern: "refreshUser\\(\\)"
    - from: "src/components/Dashboard.tsx"
      to: "Dashboard stats polling"
      via: "60-second interval for active shifts count"
      pattern: "setInterval.*60000"
    - from: "src/components/Dashboard.tsx"
      to: "Window focus event"
      via: "Immediate refresh when tab becomes active"
      pattern: "addEventListener.*focus"
---

<objective>
Unify DriverView across all user roles (ADMIN, FOREMAN, DRIVER) with real-time state synchronization after shift actions.

Purpose: Currently, drivers use DriverView component but admins/foremen cannot access it. When shift state changes (Start Shift), the UI doesn't update immediately - user must reload. This plan ensures all roles can access the unified driver interface and state updates happen instantly via refetch+refresh pattern.

Output: Updated Dashboard.tsx with DriverView access for all roles; DriverView with immediate re-render after shift actions; navigation structure updated.
</objective>

<execution_context>
@C:/Users/1/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/1/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/08-driver-ui-unification/08-CONTEXT.md

@src/components/Dashboard.tsx
@src/views/DriverView.tsx
@src/App.tsx
@src/context/AuthContext.tsx
@src/services/api.ts
</context>

<tasks>

<task type="auto">
  <name>Update Dashboard to provide DriverView access for all roles</name>
  <files>src/components/Dashboard.tsx</files>
  <action>
    Currently Dashboard.tsx only shows DriverView for DRIVER role. Update to:

    1. Add a navigation entry for "Мой рабочий день" (My Work Day) in the sidebar menu for ALL roles (ADMIN, FOREMAN, DRIVER)
    2. When this tab is active, show DriverView component regardless of role
    3. The key is: admins/foremen see THEIR OWN shifts when using DriverView (not all drivers' data)
    4. Keep existing admin dashboard view as the default "dashboard" tab
    5. The new tab can be called "driver" or "my-shifts"

    Implementation:
    - In App.tsx or Layout.tsx navigation, add a "driver" tab that maps to DriverView
    - In Dashboard.tsx, when activeTab === "driver", return <DriverView />
    - This allows admins/foremen to access the unified driver interface
    - DriverView already fetches /shifts/current which returns the current user's shift

    The goal: Anyone with DRIVER/FOREMAN/ADMIN role can access the driver interface to manage their own shifts.
  </action>
  <verify>
    1. Check that src/App.tsx or src/components/Layout.tsx has navigation entry for driver view
    2. Check that src/components/Dashboard.tsx renders DriverView when appropriate tab is active
    3. Verify: grep -r "DriverView" src/components/Dashboard.tsx shows conditional render for non-DRIVER roles
  </verify>
  <done>
    All roles (ADMIN, FOREMAN, DRIVER) can navigate to and use the DriverView component for managing their own shifts.
  </done>
</task>

<task type="auto">
  <name>Implement immediate state re-render after shift actions in DriverView</name>
  <files>src/views/DriverView.tsx</files>
  <action>
    Currently when "Start Shift" is clicked, the state updates but UI doesn't re-render immediately. The issue is that handleStart/handleEnd calls initData() but the component state doesn't trigger re-render in the same tick.

    Fix the state synchronization:

    1. In handleStart: After successful POST /shifts/start:
       - Call await initData() to refetch current shift
       - Call await refreshUser() to sync user.current_state
       - These calls should trigger re-render via state updates

    2. In handleEnd: After successful POST /shifts/end:
       - Call await initData() to refetch (should return null, setting activeShift to null)
       - Call await refreshUser() to sync user.current_state back to "idle"

    3. For demo mode (tenant_id === 999):
       - Already manually sets state with setActiveShift(mockShift)
       - Ensure this also calls refreshUser() to sync AuthContext

    The key is ensuring that after any shift action (start/end/photo), the component re-fetches shift state and updates user state, causing React to re-render with new UI.

    Current code at lines 69-120 (handleStart) and 122-154 (handleEnd) has the right pattern but may need verification that state updates trigger re-render.
  </action>
  <verify>
    1. Check that handleStart calls both initData() and refreshUser() after successful API call
    2. Check that handleEnd calls both initData() and refreshUser() after successful API call
    3. Test: grep -A5 "await api.post.*shifts/start" src/views/DriverView.tsx shows initData and refreshUser calls
  </verify>
  <done>
    After clicking "Start Shift", the UI immediately updates to show the active shift interface (Timer, Finish button) without requiring page reload.
  </done>
</task>

<task type="auto">
  <name>Implement dashboard polling and window focus refresh for real-time stats sync</name>
  <files>src/components/Dashboard.tsx</files>
  <action>
    Currently Dashboard.tsx fetches admin stats only once on mount (lines 151-172). Per Locked Decision #3 from CONTEXT.md, implement hybrid synchronization:

    1. Add 60-second polling for dashboard stats:
       - Extract the stats fetch logic into a useCallback function "fetchDashboardStats"
       - In the existing useEffect at line 151, add setInterval that calls fetchDashboardStats every 60000ms
       - Clean up interval on unmount
       - This ensures activeShifts count updates every minute

    2. Add window focus event listener for refresh:
       - In the same useEffect, add window.addEventListener('focus', fetchDashboardStats)
       - Remove listener on cleanup: window.removeEventListener('focus', fetchDashboardStats)
       - This triggers immediate refresh when user returns to the tab

    3. Keep existing 30-second interval for shift status check (refreshStatus at line 147)
       - This is separate from dashboard stats polling
       - Two independent intervals: 30s for shift status, 60s for dashboard stats
       - Both trigger on window focus

    Implementation pattern:
    ```typescript
    const fetchDashboardStats = useCallback(async () => {
      if (!isAdminView) return;
      try {
        const res = await api.get(API_ENDPOINTS.DASHBOARD_STATS);
        setStats({ /* existing state update */ });
      } catch (e) {
        console.error("Failed to fetch dashboard stats:", e);
      }
    }, [isAdminView]);

    useEffect(() => {
      // Initial fetch
      fetchDashboardStats();

      // Polling every 60 seconds
      const interval = setInterval(fetchDashboardStats, 60000);

      // Window focus refresh
      const handleFocus = () => fetchDashboardStats();
      window.addEventListener('focus', handleFocus);

      return () => {
        clearInterval(interval);
        window.removeEventListener('focus', handleFocus);
      };
    }, [fetchDashboardStats]);
    ```

    The key requirement: Dashboard stats (active shifts count) update via 60s polling + window focus trigger.
  </action>
  <verify>
    1. Check that fetchDashboardStats useCallback exists with 60s polling interval
    2. Check that window.addEventListener('focus', ...) exists for dashboard stats
    3. Verify proper cleanup in useEffect return statement
    4. Test: grep -E "setInterval.*60000|addEventListener.*focus" src/components/Dashboard.tsx shows both patterns
  </verify>
  <done>
    Dashboard stats (active shifts count) update every 60 seconds and when window/tab regains focus, ensuring admins/foremen see current data.
  </done>
</task>

</tasks>

<verification>
Phase verification:

1. Role-based access verification:
   - Log in as ADMIN - verify "My Work Day" (or equivalent) navigation exists
   - Log in as FOREMAN - verify navigation access exists
   - Log in as DRIVER - verify existing access still works

2. State sync verification:
   - Click "Start Shift" - verify UI transitions to active shift view immediately
   - Click "Finish Shift" - verify UI returns to selection screen immediately
   - No page reload required for state transitions

3. Demo mode verification:
   - As demo tenant (999), start shift - verify state updates immediately
   - Confirm demo mode uses same UI flow as production
</verification>

<success_criteria>
1. DriverView is accessible from navigation for ALL user roles (ADMIN/FOREMAN/DRIVER)
2. Each role sees only their own shift data when using DriverView
3. Shift state changes (Start/End) trigger immediate UI re-render without page reload
4. Dashboard stats (active shifts count) poll every 60 seconds for admins/foremen
5. Dashboard stats refresh immediately when window/tab regains focus
6. Demo mode (Tenant 999) follows same state machine as production
</success_criteria>

<output>
After completion, create `.planning/phases/08-driver-ui-unification/08-01-SUMMARY.md`
</output>
