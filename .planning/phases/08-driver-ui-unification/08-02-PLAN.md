---
phase: 08-driver-ui-unification
plan: 02
type: execute
wave: 2
depends_on: [08-01]
files_modified:
  - src/views/DriverView.tsx
autonomous: true

must_haves:
  truths:
    - "Demo Driver mode (Tenant 999) displays production DriverView UI with identical functionality"
    - "Demo mode shifts follow same state machine as production (idle -> active/awaiting_odo -> etc)"
    - "No safety guards or restrictions prevent demo mode users from experiencing full driver workflow"
    - "Demo mode is auto-detected by tenant_id === 999"
  artifacts:
    - path: "src/views/DriverView.tsx"
      provides: "Production DriverView with demo mode auto-detection and full simulation"
      min_lines: 400
  key_links:
    - from: "src/views/DriverView.tsx"
      to: "user.tenant_id"
      via: "Check tenant_id === 999 to enable demo mode simulation"
      pattern: "tenant_id.*999|tenantId.*999"
    - from: "src/views/DriverView.tsx"
      to: "localStorage mock shift data"
      via: "Store demo shift in localStorage for persistence across renders"
      pattern: "localStorage\\.setItem.*active_shift"
---

<objective>
Ensure Demo Driver mode (Tenant 999) provides full simulation of production DriverView with identical UI and no safety restrictions.

Purpose: Demo mode should give users the complete experience of using LogiShift as a driver. Per user decision, NO safety guards - full simulation identical to production. Demo mode is auto-detected by tenant_id === 999 and uses the same DriverView component with mock data that mimics production behavior.

Output: Verified DriverView demo mode implementation with full state machine simulation.
</objective>

<execution_context>
@C:/Users/1/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/1/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/08-driver-ui-unification/08-CONTEXT.md

@src/views/DriverView.tsx
@.planning/phases/08-driver-ui-unification/08-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Verify and document demo mode implementation in DriverView</name>
  <files>src/views/DriverView.tsx</files>
  <action>
    Review the existing demo mode implementation (lines 69-103 for handleStart, lines 122-140 for handleEnd):

    1. Verify demo detection: `if (user?.tenant_id === 999)`
    2. Confirm demo mode creates mock shift object with production-like structure
    3. Confirm demo mode advances state machine identically to production
    4. Check for any safety guards or restrictions that should be removed per user decision

    Current implementation should:
    - Create mock shift with id, status, start_time, truck, site objects
    - Set activeShift state with mock data
    - Update user.current_state based on site requirements
    - Use same UI rendering as production (no conditional demo-only UI)
    - Persist to localStorage for continuity

    If any restrictions exist (like "Demo mode - data not saved" warnings), remove them. Per user decision: "NO safety guards - full simulation identical to production"

    The demo mode should feel exactly like using the production app, just with mock data that doesn't hit the backend.
  </action>
  <verify>
    1. Check that tenant_id === 999 detection exists in DriverView
    2. Check that demo mode creates properly structured mock shift objects
    3. Verify no UI elements are conditionally hidden/restricted for demo mode
    4. Confirm state machine transitions match production logic
  </verify>
  <done>
    Demo Driver mode provides full simulation of production DriverView with identical UI and workflow. No restrictions or safety guards exist.
  </done>
</task>

</tasks>

<verification>
Demo mode verification:

1. Access demo tenant (999) as any role:
   - Verify DriverView loads with full interface
   - Verify truck/site selection works
   - Verify "Start Shift" creates active shift with Timer display
   - Verify "Finish Shift" returns to selection screen
   - Verify photo upload workflow (if site requires odometer/invoice)

2. Compare with production:
   - UI should be identical between demo and production
   - Only difference is data source (mock vs API)
   - No "DEMO MODE" badges or warnings in DriverView (those are in Layout)
</verification>

<success_criteria>
1. Demo mode (Tenant 999) auto-detected and uses production DriverView
2. Full state machine simulation available in demo mode
3. No UI restrictions or safety guards prevent demo workflow
4. Demo shifts persist in localStorage for session continuity
</success_criteria>

<output>
After completion, create `.planning/phases/08-driver-ui-unification/08-02-SUMMARY.md`
</output>
