---
phase: 10-dashboard-analytics-audit
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/Dashboard.tsx
autonomous: true

must_haves:
  truths:
    - Dashboard admin view has a manual refresh button
    - Clicking refresh button updates all stats (activeShifts, activeDrivers, usage)
    - Refresh button shows loading state during fetch
    - All three stats sections update: main cards, usage limits, active shifts details
  artifacts:
    - path: "src/components/Dashboard.tsx"
      provides: "Dashboard with manual refresh capability"
      contains: "handleManualRefresh, refresh button"
  key_links:
    - from: "Dashboard refresh button click"
      to: "fetchDashboardStats()"
      via: "onClick handler"
      pattern: "onClick.*fetchDashboardStats"
    - from: "fetchDashboardStats()"
      to: "/api/v1/dashboard/stats"
      via: "api.get()"
      pattern: "api\\.get\\(API_ENDPOINTS\\.DASHBOARD_STATS\\)"
---

<objective>
**Goal:** Implement manual refresh mechanism for dashboard stats

**Purpose:** Allow admins to manually refresh dashboard statistics on demand, not just rely on 60s polling.

**Output:** Dashboard has a refresh button that updates all statistics when clicked.
</objective>

<execution_context>
@C:/Users/1/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/1/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

# Key Files
@src/components/Dashboard.tsx (dashboard with 60s polling, no manual refresh)
@src/components/Analytics.tsx (has refresh button pattern to reference)

# Pattern from Analytics.tsx:
- Refresh button with RefreshCw icon
- Loading state with spinner animation
- Calls fetchAll functions on click
</context>

<tasks>

<task type="auto">
  <name>Add manual refresh button to Dashboard admin view</name>
  <files>src/components/Dashboard.tsx</files>
  <action>
    1. Import RefreshCw icon from lucide-react (already imported, verify)
    2. Add `isRefreshing` state variable:
       ```typescript
       const [isRefreshing, setIsRefreshing] = useState(false);
       ```
    3. Create `handleManualRefresh` function:
       ```typescript
       const handleManualRefresh = async () => {
         setIsRefreshing(true);
         try {
           await fetchDashboardStats();
         } finally {
           setIsRefreshing(false);
         }
       };
       ```
    4. Add refresh button in header area (near "Лимиты тарифа" section or as top-right button):
       - Use RefreshCw icon
       - Add `animate-spin` class when `isRefreshing` is true
       - Position near the usage section header for visibility
       - Style matching existing theme: bg-slate-100 hover:bg-slate-200, rounded

       Button HTML:
       ```tsx
       <button
         onClick={handleManualRefresh}
         disabled={isRefreshing}
         className="p-2 bg-slate-100 hover:bg-slate-200 rounded-lg transition-colors disabled:opacity-50"
         title="Обновить статистику"
       >
         <RefreshCw className={`w-4 h-4 ${isRefreshing ? 'animate-spin' : ''}`} />
       </button>
       ```
    5. Place button in the flex header with "Лимиты тарифа" heading:
       - Update header div to include refresh button alongside the title
       - Use existing pattern from System.tsx (line 289-297) as reference
  </action>
  <verify>
    - Refresh button is visible in Dashboard admin view
    - Clicking refresh button triggers data fetch
    - Button shows spinning animation during refresh
    - All stats update after refresh completes
  </verify>
  <done>
    Dashboard has working manual refresh button that updates all stats
  </done>
</task>

</tasks>

<verification>
1. Navigate to Dashboard as ADMIN user
2. Locate the refresh button (should be near "Лимиты тарифа" section)
3. Click refresh button
4. Verify spinning animation shows during fetch
5. Verify all stats update: active shifts, active drivers, usage limits
</verification>

<success_criteria>
- Manual refresh button visible in Dashboard admin view
- Button updates all dashboard statistics on click
- Loading state visible during refresh
- No API errors on refresh
</success_criteria>

<output>
After completion, create `.planning/phases/10-dashboard-analytics-audit/10-02-SUMMARY.md`
</output>
