---
phase: 10-dashboard-analytics-audit
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/Dashboard.tsx
  - src/services/api.ts
autonomous: true

must_haves:
  truths:
    - Dashboard active shifts count displays correct value from backend API
    - activeDrivers count matches backend data
    - Dashboard stats cards show real data, not hardcoded zeros
  artifacts:
    - path: "src/components/Dashboard.tsx"
      provides: "Dashboard stats display with correct data mapping"
      contains: "activeShifts, activeDrivers, fetchDashboardStats"
    - path: "src/services/api.ts"
      provides: "API request handling for dashboard stats"
      exports: ["get"]
  key_links:
    - from: "src/components/Dashboard.tsx"
      to: "/api/v1/dashboard/stats"
      via: "api.get(API_ENDPOINTS.DASHBOARD_STATS)"
      pattern: "fetchDashboardStats|setStats"
---

<objective>
**Goal:** Audit and fix Dashboard data mapping for accurate active shifts count

**Purpose:** Fix the discrepancy where Dashboard shows "0" active shifts when shifts actually exist in the backend.

**Output:** Dashboard displays accurate counts for active shifts, active drivers, and trucks in work.
</objective>

<execution_context>
@C:/Users/1/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/1/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

# Key Files
@src/components/Dashboard.tsx (dashboard stats fetching and display)
@src/services/api.ts (API client)
@src/constants.ts (API endpoints)
@src/types.ts (TypeScript interfaces)
</context>

<tasks>

<task type="auto">
  <name>Audit Dashboard data mapping and API response structure</name>
  <files>src/components/Dashboard.tsx</files>
  <action>
    1. Examine current Dashboard.tsx data mapping in `fetchDashboardStats()` function
    2. Check API endpoint: `API_ENDPOINTS.DASHBOARD_STATS` = `/api/v1/dashboard/stats`
    3. Review response field mapping:
       - `activeShifts: res.activeShifts || 0`
       - `activeDrivers: res.activeDrivers || 0`
       - `trucksInWork: res.trucksInWork`
    4. Compare with backend response structure by checking for camelCase vs snake_case differences
    5. Add console.log debugging to capture actual API response structure
    6. Fix mapping mismatches if backend uses different field names (e.g., `active_shifts` vs `activeShifts`)

    Common mapping patterns to verify:
    - Backend snake_case: `active_shifts`, `active_drivers`, `trucks_in_work`
    - Frontend expects: `activeShifts`, `activeDrivers`, `trucksInWork`

    Fix by updating mapping to handle both formats:
    ```typescript
    activeShifts: res.activeShifts ?? res.active_shifts ?? 0,
    activeDrivers: res.activeDrivers ?? res.active_drivers ?? 0,
    trucksInWork: res.trucksInWork ?? res.trucks_in_work
    ```
  </action>
  <verify>
    - Check browser console for logged API response structure
    - Verify Dashboard displays non-zero values when shifts exist
    - Inspect Network tab response for `/api/v1/dashboard/stats`
  </verify>
  <done>
    Dashboard displays accurate active shifts count matching backend data
  </done>
</task>

</tasks>

<verification>
1. Navigate to Dashboard as ADMIN user
2. Observe "Активные смены" card shows correct count (not 0 when shifts exist)
3. Check "Водителей в рейсе" card shows accurate count
4. Verify no console errors related to undefined properties
</verification>

<success_criteria>
- Dashboard active shifts count matches actual backend data
- No discrepancy showing "0" when shifts exist
- Data mapping handles both camelCase and snake_case from backend
</success_criteria>

<output>
After completion, create `.planning/phases/10-dashboard-analytics-audit/10-01-SUMMARY.md`
</output>
